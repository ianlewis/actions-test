name: Test workflow
on:
  workflow_dispatch:

permissions: read-all

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - run: |
          url=$(gh issue create --title "Test issue" --body "Test issue")
          echo "${url}"
          gh issue close "${url}"
